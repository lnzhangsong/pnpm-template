{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "prestart": "webpack --config webpack.config.js",
    "start:server": "webpack server --config webpack.config.js",
    "start:fisrt": "pnpm prestart && wait-on dist && electron .",
    "start:direct": "electron .",
    "start:render": "wait-on http://localhost:3000 && electron ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^5.0.7",
    "source-map-loader": "^0.2.4",
    "ts-loader": "^6.0.4",
    "typescript": "^3.5.3",
    "webpack": "^5.70.0",
    "webpack-cli": "^4.9.2"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0",
    "render": "workspace:^0.1.0",
    "service": "workspace:^1.0.0"
  }
}
